{
  "name": "ttc-ride",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "export": "next build && next export -o dist",
    "deploy": "npm run export && npx surge --domain `cat CNAME` dist"
  },
  "dependencies": {
    "next": "10.0.4",
    "react": "17.0.1",
    "react-dom": "17.0.1",
    "react-player": "^2.7.2",
    "surge": "^0.23.0"
  }
}
